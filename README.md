# oh-my-skills
保存个人常用skills

## 1 record-code-change
- 功能：使用obsidian cli在开发后自动保存代码变更内容
- 提示：为了让它更容易自动触发，你可以在仓库的 AGENTS.md 或 CLAUDE.md 里加一句：
"发生代码变更并准备结束任务时，必须调用 `record-code-change`，将本次改动写入 Obsidian 版本变更文件。"
这样 skill 命中率会高很多。